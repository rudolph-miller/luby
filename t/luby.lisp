(in-package :cl-user)
(defpackage luby-test
  (:use :cl
        :luby
        :prove))
(in-package :luby-test)

;; NOTE: To run this test file, execute `(asdf:test-system :luby)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
